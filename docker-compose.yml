version: '3'
services:
  web:
    image: java:8-jdk-alpine
    ports:
      - "8080:8080"
    volumes:
      - ./no-escalapp-master:/app
    working_dir: /app
    command: ./mvnw spring-boot:run
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M

